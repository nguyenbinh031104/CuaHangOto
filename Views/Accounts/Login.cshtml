@model CuaHangOto.Models.KhachHang

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Login</h2>
@*<table align="center">
    <h2 style="text-align:center">Dang Nhap</h2>

    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)
        {
            <tr style="height:40px">
                <td>Ten dang nhap: </td>
                <td>@Html.TextBox("Email") @ViewData["Loi1"]</td>
            </tr>
            <tr style="height:40px">
                <td>Mat khau: </td>
                <td>@Html.Password("MatKhau") @ViewData["Loi2"]</td>
            </tr>

            <tr style="height:40px">

                <td><input type="submit" name="Dang nhap" /></td>
            </tr>
        }
    }
</table>*@

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Email, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.Email, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Password", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.MatKhau, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.MatKhau, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Login" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Click here to Signup", "Signup")
</div>

